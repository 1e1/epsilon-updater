---
name: Rapport de bug
about: Un comportement incorrect (démo virtuelle ou matériel réel)
title: "[bug] "
labels: bug
---

> ⚠️ **Ne collez aucun secret** : jeton `remember_user_token`, n° de série, ou dump non
> caviardé (`nwupdater-capture` produit des dumps — utilisez `nwupdater-capture scrub`).
> Pour un problème de **sécurité**, passez plutôt par [SECURITY.md](../SECURITY.md).

## Description

<!-- Ce qui se passe vs ce qui est attendu. -->

## Reproduction

1. Commande exacte (ex. `nwupdater install --virtual n0110 --to-version 25.2.0`)
2. …

## Contexte

- Version de `nwupdater` (`nwupdater --help` / `pip show nwupdater`) :
- Device : [ ] virtuel `--virtual nXXXX`  [ ] matériel réel (modèle : )
- OS + Python (`python3 --version`) :
- Navigateur (si UI web) :

## Logs / sortie

```
(coller ici la sortie — sans secret)
```
